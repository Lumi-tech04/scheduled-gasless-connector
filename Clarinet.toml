[project]
name = "scheduled-gasless-connector"
authors = ["Web3 DevTools Team"]
description = "Automated scheduling and payment routing with minimal friction"
telemetry = false
requirements = []
[contracts.scheduled-gasless-connector]
path = "contracts/scheduled-gasless-connector.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
